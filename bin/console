<?php

require_once __DIR__ . '/../vendor/autoload.php';

use App\Console\Application;
use App\Console\Command\HelloWorldCommand;
use App\Console\Command\RunWorkflowCommand;
use App\Console\Command\TestModelCommand;

$app = new Application();
$app->addCommand(new HelloWorldCommand());
$app->addCommand(new RunWorkflowCommand());
$app->addCommand(new TestModelCommand());

$app->run($argv);